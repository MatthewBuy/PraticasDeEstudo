using System;
using System.Collections.Generic;
using System.Text;

namespace PetShop
{
    class Animals
    {

        public string DogBreed { get; set; }
        public string DogSize { get; set; }

     

        


        /*public Animals (string dog, string size)
        {
            this.DogBreed = dog;
            this.DogSize = size;
        }


        public void CalculateThePrice(string dog, string size)
        {
            decimal price;
            
            if(dog == "Beagle" && size == "Medium")
            {
                Console.WriteLine($"The price is R${price = 30} reais");
            }
            else if (dog == "Beagle" && size == "Small")
            {
                Console.WriteLine($"The price is R${price = 15} reais");
            }else
            {
                 Console.WriteLine($"The price is R${price = 50} reais");
            }


        }*/

    }
}
